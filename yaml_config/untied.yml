model:
  untied_structure_module: true
  evoformer_stack:
    no_blocks: 8
  structure_module:
    no_blocks: 4
  template:
    enabled: false
  extra_msa:
    enabled: false

scheduler:
  warmup_no_steps: 5000
  start_decay_after_n_steps: 10000
  decay_every_n_steps: 1000