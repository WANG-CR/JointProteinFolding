model:
  template:
    enabled: true
  heads:
    tm:
      enabled: true

data:
  common:
    reduce_max_clusters_by_max_templates: true
    use_templates: true
    use_template_torsion_angles: true

loss:
  tm:
    weight: 0.1